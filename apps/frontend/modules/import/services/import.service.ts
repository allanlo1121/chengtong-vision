import { ImportConfig } from "../types";

export async function importData(data: any[], config: ImportConfig) {
  // 1️⃣ 准备数据
  const inserts: any[] = [];
  const updates: any[] = [];
  // 2️⃣ 持久化

  return result;
}
