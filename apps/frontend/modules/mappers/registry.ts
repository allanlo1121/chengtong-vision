// modules/mappers/registry.ts

// import { MapperOptions } from "@/lib/utils/data-mapper";
// import { TableName, Entity } from "@/lib/core/types/entity.types";

// const registry = new Map<TableName, MapperOptions<any>>();

// export function registerMapper<T extends TableName>(
//     table: T,
//     config: MapperOptions<Entity<T>>
// ) {
//     registry.set(table, config);
// }

// export function getMapper<T extends TableName>(
//     table: T
// ): MapperOptions<Entity<T>> {
//     return registry.get(table) || {};
// }
