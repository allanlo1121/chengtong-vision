// import { registerMapper } from "./registry";

// registerMapper("import_records", {
//     transform: {
//         importJson: {
//             toDb: (v) => JSON.stringify(v),
//             fromDb: (v) => JSON.parse(v),
//         },
//         mappedJson: {
//             toDb: (v) => JSON.stringify(v),
//             fromDb: (v) => JSON.parse(v),
//         },
//         errorJson: {
//             toDb: (v) => JSON.stringify(v),
//             fromDb: (v) => JSON.parse(v),
//         },
//     },
// });
